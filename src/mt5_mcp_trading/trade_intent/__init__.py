"""Signal -> TradeIntent assembly. No adapter imports, ever (enforced by
tests/test_architecture.py). Intentionally empty in Phase 2; populated in Phase 4/5 alongside
strategy migration and the dry-run pipeline."""
