"""In-memory implementations of the adapter interfaces, for tests only.

Never wired into anything that could reach a real MT5 terminal or MCP server. Used directly
by tests, and by MOCK execution mode once the composition root exists (Phase 5+).
"""
