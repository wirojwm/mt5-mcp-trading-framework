"""Pure feature math (EMA, ATR, MACD, ...) over MarketBar sequences. No adapter imports, ever.

ema (ema.py), atr (atr.py), and macd (macd.py) ported from the legacy project in Phase 4, as
tested, dependency-free pure Python -- not by copying the legacy pandas/list-based
implementations verbatim.
"""
