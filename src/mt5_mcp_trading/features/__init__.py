"""Pure feature math (EMA, ATR, MACD, ...) over MarketBar sequences. No adapter imports, ever.

ema (ema.py) and atr (atr.py) ported from the legacy project in Phase 4, as tested,
dependency-free pure Python -- not by copying the legacy pandas-based implementations.
MACD is not yet ported (belongs to the runner strategy, migrated in a later Phase 4 step).
"""
