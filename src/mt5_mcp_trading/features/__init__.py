"""Pure feature math (EMA, ATR, MACD, ...) over MarketBar sequences. No adapter imports, ever.

Intentionally empty in Phase 2. Populated in Phase 4 by porting the reusable math identified
in the Phase 0 legacy audit (bbn_grid.py, launcher_grid.py, ema_crossover_core_multi.py) as
tested pure functions, not by copying the legacy implementations.
"""
