"""Signal assembly helpers. No adapter imports, ever (enforced by tests/test_architecture.py).

Intentionally empty in Phase 2. The Signal type itself already exists in domain.models;
this package is for logic that builds/combines signals, added alongside strategy migration
in Phase 4.
"""
