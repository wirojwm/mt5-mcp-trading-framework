"""Pure functions: FeatureSet -> Signal. No adapter imports, ever (enforced by
tests/test_architecture.py). Intentionally empty in Phase 2; populated one component at a
time in Phase 4 (grid, then runner, then guard trigger logic)."""
