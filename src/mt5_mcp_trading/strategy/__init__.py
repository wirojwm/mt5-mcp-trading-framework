"""Pure strategy functions: bars/features -> Signal or strategy-specific output (e.g.
GridLevels). No adapter imports, ever (enforced by tests/test_architecture.py).

grid.py and runner.py ported in Phase 4 (see each module's docstring for exactly what was
and wasn't preserved from the legacy project). Guard trigger logic is not yet ported.
"""
